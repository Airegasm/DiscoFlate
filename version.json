{
  "version": "3.44.4",
  "versionCode": 118,
  "apk_url": "https://github.com/Airegasm/DiscoFlate/releases/download/v3.44.4/discoflate.apk",
  "notes": "DiscoFlate v3.44.4 \u2014 local auth no longer depends on browser cookies: the panel page carries the auth secret and sends it as a header on every call, so cookie-blocking browsers (Opera/Opera GX shields, strict privacy modes, embedded docks) can save the token and use sensitive endpoints normally. The old 'missing local auth' failure also now explains the fix (reload the tab) instead of just failing."
}
