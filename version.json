{
  "version": "3.44.0",
  "versionCode": 114,
  "apk_url": "https://github.com/Airegasm/DiscoFlate/releases/download/v3.44.0/discoflate.apk",
  "notes": "DiscoFlate v3.44.0 — minigame tiers join the action system (config v11): a tier is just 'score op N' + an action block (its old result message and device fires migrate to message/fire rows; the game still posts its labeled, anonymity-safe score line). Big UI compaction pass from live feedback: every action row and tier is collapsible to a one-line summary with a gist; block sections collapse with counts; all card titlebars keep their buttons on one row; Always-On, events, polls, competitions, capacity events, prizes and system commands use compact one-row parameter layouts (no more full-page number boxes — the phone-width CSS that exploded rows is gone); System commands is collapsible with each command's reply next to its name; roll actions use dice/sides dropdowns; competitions put Win by, rerolls and Add-ALL-totals on the parameter row; Competitions and Bonus Rounds are separate cards. Everything auto-migrates."
}
