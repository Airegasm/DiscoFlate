{
  "version": "3.7.0",
  "versionCode": 61,
  "apk_url": "https://github.com/Airegasm/DiscoFlate/releases/download/v3.7.0/discoflate.apk",
  "notes": "DiscoFlate v3.7.0 — STOP now truly stops (and pauses the whole session with a RESUME button); failsafe OFF watchdog; automatic config backups + corrupt-config recovery; stale-tab save protection; security hardening (credential masking, host pinning, local auth); dozens of correctness fixes. First release-signed APK: this ONE update requires uninstall/reinstall (export your config first via Help → Export config!)."
}
